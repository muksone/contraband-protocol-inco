import "./globals.css";
import "@rainbow-me/rainbowkit/styles.css";
import type { Metadata } from "next";
import localFont from "next/font/local";
import { Providers } from "@/components/Providers";
import { Header } from "@/components/Header";
import { Toaster } from "sonner";

// DepartureMono - the pixel-mono face from the hangman/mines apps.
const departureMono = localFont({
  src: "./fonts/DepartureMono-Regular.woff",
  variable: "--font-mono",
  display: "swap",
});

const TITLE = "Contraband Protocol | Inco";
const DESCRIPTION =
  "A hidden-cargo bluffing game on Inco. Declare the manifest, pass the ship, or break the seal.";

// og/twitter images are generated by app/opengraph-image.tsx + twitter-image.tsx.
export const metadata: Metadata = {
  metadataBase: new URL("https://contraband.polkahub.xyz"),
  title: TITLE,
  description: DESCRIPTION,
  openGraph: {
    title: TITLE,
    description: DESCRIPTION,
    type: "website",
    url: "/",
    siteName: "Contraband Protocol",
  },
  twitter: {
    card: "summary_large_image",
    title: TITLE,
    description: DESCRIPTION,
    site: "@inconetwork",
    creator: "@inconetwork",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html suppressHydrationWarning lang="en" className="dark">
      <body suppressHydrationWarning className={`min-h-screen font-mono ${departureMono.variable}`}>
        <Providers>
          <Header />
          {children}
        </Providers>
        <Toaster
          position="bottom-center"
          toastOptions={{
            style: {
              background: "hsl(var(--card))",
              color: "hsl(var(--foreground))",
              border: "1px solid hsl(var(--border))",
              fontFamily: "var(--font-mono), ui-monospace, monospace",
              fontSize: "13px",
              borderRadius: "0",
            },
          }}
        />
      </body>
    </html>
  );
}
